# Antigravity Control Channel

This branch is reserved for ChatGPT ↔ Antigravity plan-only task exchange.

- `tasks/*.json` — task requests created by ChatGPT.
- `results/*.json` — task results written by the local watcher.
- This branch must never be merged into `main`.
- Initial mode is PLAN ONLY. No file edits are authorized.
