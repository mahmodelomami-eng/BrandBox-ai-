{
  "id": "task-018-phase1a-rbac-foundation-retry",
  "mode": "accept-edits",
  "effort": "medium",
  "prompt": "Work only in the isolated BrandBox-UI-Restore workspace on branch feat/admin-rbac-foundation. Implement Phase 1A only. Scope: database enum expansion + RBAC foundation + compatibility tests. Do not build Users UI or roles UI yet. Required: 1) add a new Supabase migration that extends public.app_role with PLATFORM_ADMIN, OPERATIONS_MANAGER, CONTENT_MANAGER, USER_MANAGER, SUPPORT_AGENT, FINANCE_MANAGER, MARKETING_MANAGER, SECURITY_AUDITOR, ANALYST using ADD VALUE IF NOT EXISTS, preserving existing SUPER_ADMIN, ADMIN, SUPPORT, USER values; do not create OWNER as an assignable app_role; 2) refactor src/lib/auth/rbac-engine.ts into a backward-compatible engine with modern resource.action permissions, role labels/descriptions in Arabic and English, role risk/access metadata, and a compatibility map for existing uppercase permissions so current code keeps working; 3) keep legacy ADMIN and SUPPORT supported as aliases/compatibility roles while adding PLATFORM_ADMIN and SUPPORT_AGENT; 4) define least-privilege role matrices, with SECURITY_AUDITOR and ANALYST read-only, SUPPORT_AGENT limited, and SUPER_ADMIN full platform permissions; 5) expose helpers suitable for future UI such as getRoleDefinition/getRolePermissions/isAssignableRole/checkModernPermission while preserving existing checkPermission/assertPermission APIs; 6) add or update focused tests for permission mappings, legacy compatibility, read-only auditor/analyst behavior, and OWNER exclusion from assignable roles. Do not edit Admin Users API/UI yet. Do not touch payments, Ezone Pay, generation engine, WhatsApp, Store, deployment, production, or secrets. You may use only the already-allowed read-only git checks and npm test/build commands; do not run git add/commit/push/pull/fetch/merge/rebase/reset/checkout/switch. After edits run git diff --check, npm.cmd test, npm.cmd run test:focused, and npm.cmd run build if allowed. Return exact files changed, migration safety notes, test/build results, and any compatibility risk."
}