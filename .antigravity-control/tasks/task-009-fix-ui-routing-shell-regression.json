{
  "id": "task-009-fix-ui-routing-shell-regression",
  "mode": "accept-edits",
  "effort": "high",
  "prompt": "Work ONLY in the explicitly added isolated BrandBox-UI-Restore workspace on fix/restore-approved-workspace-ui, preserving existing task-005/006/007 edits unless they caused the observed regression. The Vercel Preview proves the restoration is NOT yet correct. User-observed failures: (A) /dashboard still renders the wrong legacy/WorkspaceLayout-style dashboard with right sidebar and old compact header instead of the previously approved StableUserDashboard design; (B) the approved persistent GlobalNavigation top bar is absent on dashboard; (C) Images AI routing is inconsistent: selecting Images AI from the approved AI tools navigation opens the approved image projects/workspace flow, but selecting Images AI from dashboard quick actions and from the currently visible sidebar opens a different/legacy image screen. Fix the root routing/layout composition, not CSS symptoms. First inspect route/layout hierarchy and wrappers: src/app/layout.*, any route-group layouts, src/app/dashboard/page.jsx, AppNavigationWrapper.jsx, WorkspaceLayout.jsx, WorkspaceDashboardShell.jsx, StableUserDashboard.jsx, GlobalNavigation.jsx, ToolProjectsWorkspace and all Images links/hrefs in dashboard/sidebar/navigation. Determine why the Preview still shows WorkspaceLayout chrome despite prior edits. Required final behavior: 1) /dashboard renders the approved StableUserDashboard content, not the legacy dashboard shown in Preview; 2) GlobalNavigation is the ONE persistent top navigation on dashboard and all normal user workspace screens; 3) no persistent right/left WorkspaceLayout sidebar and no duplicate compact workspace header on these screens; 4) EVERY Images AI entry point—GlobalNavigation AI tools, dashboard quick action/card, any remaining navigation entry, /images-ai compatibility URL—must converge to the SAME canonical /projects/images flow; opening a project must converge to /projects/images/workspace?project=<real-id>; 5) preserve real AuthContext, notifications, user/profile, credits, RBAC/admin access, Supabase project IDs, generation API and credit logic; 6) do not redesign the approved UI; reuse existing approved components; 7) remove or bypass obsolete wrappers only when necessary, without breaking admin/public routes; 8) search for all href/router.push/redirect references to /images-ai and legacy Images routes and normalize them; 9) inspect Next.js layout nesting so the fix actually appears in Vercel Preview, not just locally in an unused component. Do not touch payments, Ezone Pay, migrations, WhatsApp, Store, Supabase schema/RLS, generation engine internals, secrets, deployment config, production, or main. Do NOT run git commit/push/pull/merge/rebase/reset/checkout/switch. You may edit source in the isolated workspace. After editing, use file inspection/search to verify no conflicting Images entry point remains and no WorkspaceLayout sidebar/header wraps /dashboard or /projects/images. If permitted, run targeted tests/build; otherwise report not run. Return exact files changed, root cause, canonical routes, and any remaining visual risk."
}