{
  "id": "task-005-restore-dashboard-shell",
  "mode": "accept-edits",
  "effort": "high",
  "prompt": "Work ONLY in the explicitly added BrandBox-UI-Restore workspace. This is an isolated restoration branch. Restore the previously approved Brand Box workspace UI with the smallest safe change set. Use existing components already present in the repository rather than redesigning from scratch. Primary goals for this task: (1) make the approved StableUserDashboard the dashboard experience at /dashboard, preserving real AuthContext/Supabase-backed user, credits and project behavior instead of reintroducing mock identity/session data; (2) make GlobalNavigation the persistent top navigation across workspace screens as requested, while preventing any double header; (3) remove/disable the WorkspaceLayout sidebar and its duplicate header/chrome from the workspace presentation, but preserve route protection, AuthContext, notifications, account/admin access, RBAC and responsive behavior; (4) do not touch payments, Ezone Pay, database migrations, WhatsApp, Store, Supabase schema/RLS, generation engines, secrets, deployment config, or production; (5) do NOT run git commit, git push, git pull, git merge, git rebase, git reset, git checkout, git switch, deployment, migrations, or destructive commands. You may edit source files in the isolated workspace. Before editing, read the relevant existing files including StableUserDashboard.jsx, GlobalNavigation.jsx, WorkspaceLayout.jsx, AppNavigationWrapper.jsx, src/app/dashboard/page.jsx and AuthContext.jsx. Preserve real modern routing and data wiring. After edits, inspect your changes for duplicate navigation and obvious import/runtime issues. If command permission prevents tests/build, do not broaden permissions; report that tests were not run. Return a concise summary of files changed, exact behavior restored, any remaining work for Images AI restoration, and risks. Do not modify Images AI in this task unless a tiny routing dependency is strictly required for navigation consistency."
}