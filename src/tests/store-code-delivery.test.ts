import{readFileSync}from'node:fs';import{join}from'node:path';import assert from'node:assert/strict';const r=process.cwd();const m=readFileSync(join(r,'supabase/migrations/20260831014500_store_atomic_code_delivery.sql'),'utf8');const f=readFileSync(join(r,'src/lib/store/store-code-fulfillment.ts'),'utf8');const s=readFileSync(join(r,'src/lib/store/store-service.ts'),'utf8');assert.ok(m.includes('for update skip locked'));assert.ok(m.includes("status='RESERVED'"));assert.ok(m.includes("status='DELIVERED'"));assert.ok(m.includes('service_role'));assert.ok(f.includes('reserve_store_digital_codes'));assert.ok(f.includes('deliver_store_digital_codes'));assert.ok(f.includes("entitlement_type:'VOUCHER'"));assert.ok(s.includes('processDigitalCodeFulfillmentForOrder(orderId)'));console.log('Store atomic digital code delivery tests passed.');