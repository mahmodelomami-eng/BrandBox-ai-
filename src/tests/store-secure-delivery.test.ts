import{readFileSync}from'node:fs';import{join}from'node:path';import assert from'node:assert/strict';const r=process.cwd();const api=readFileSync(join(r,'src/app/api/v1/store/delivery/route.ts'),'utf8');const ui=readFileSync(join(r,'src/app/store/purchases/page.jsx'),'utf8');assert.ok(api.includes(".eq('user_id',user.id)"));assert.ok(api.includes("data.status!=='ACTIVE'"));assert.ok(api.includes("'Cache-Control':'private, no-store"));assert.ok(api.includes("data.entitlement_type==='VOUCHER'"));assert.ok(ui.includes('revealDelivery'));assert.ok(ui.includes('عرض بيانات التسليم'));console.log('Store secure purchases delivery tests passed.');