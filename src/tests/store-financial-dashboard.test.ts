import {readFileSync} from 'node:fs';import{join}from'node:path';import assert from'node:assert/strict';const r=process.cwd();const api=readFileSync(join(r,'src/app/api/v1/admin/store/finance/route.ts'),'utf8'),p=readFileSync(join(r,'src/components/AdminStoreFinancialPanel.jsx'),'utf8'),c=readFileSync(join(r,'src/components/AdminControlCenter.jsx'),'utf8');assert.ok(api.includes("checkPermission(role,'payments.read')"));assert.ok(api.includes('providerCostLYD'));assert.ok(api.includes('grossMarginPercent'));assert.ok(p.includes('ربحية المنتجات وSKU'));assert.ok(p.includes('قبل المصروفات التشغيلية والضرائب'));assert.ok(c.includes('<AdminStoreFinancialPanel/>'));console.log('Store finance dashboard tests passed.');