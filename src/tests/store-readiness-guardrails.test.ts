import{readFileSync}from'node:fs';import{join}from'node:path';import assert from'node:assert/strict';const r=process.cwd();const api=readFileSync(join(r,'src/app/api/v1/admin/store/operations/route.ts'),'utf8'),ui=readFileSync(join(r,'src/components/AdminStoreOperationsPanel.jsx'),'utf8');for(const x of ['supplierAuthorized','regionVerified','fulfillmentVerified','providerActive','activeSku','providerMapping','sellableMode'])assert.ok(api.includes(x));assert.ok(api.includes("Object.values(checks).every(Boolean)"));assert.ok(ui.includes('جاهزية الكتالوج للبيع'));assert.ok(ui.includes('منتجات جاهزة'));console.log('Store readiness guardrail tests passed.');